Перем ЗапрещенныеСчета Экспорт;
Перем СписокИсключений Экспорт;

Процедура ПередЗаписью(Отказ)

	Если ОбменДанными.Загрузка Тогда
		Возврат;
	КонецЕсли;
	
	Порядок = ?(Забалансовый или Код="000","ЗБ","")+сокрлп(ПолучитьПорядокКода());

КонецПроцедуры

ЗапрещенныеСчета = Новый СписокЗначений();
//ЗапрещенныеСчета.Добавить(ПланыСчетов.Хозрасчетный.ОборудованиеКУстановке);

СписокИсключений = Новый СписокЗначений();
//СписокИсключений.Добавить(ПланыСчетов.Хозрасчетный.ВекселяВыданные);

