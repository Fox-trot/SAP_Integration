
Процедура ВывестиСообщениеПользователю(Знач Текст, Объект=Неопределено, Поле=Неопределено) Экспорт
	
	СообщениеПользователю = Новый СообщениеПользователю;
	СообщениеПользователю.Текст = Текст;
	Если Поле<>Неопределено Тогда
		СообщениеПользователю.Поле = Поле;
	КонецЕсли;
	Если Объект<>Неопределено Тогда
		СообщениеПользователю.УстановитьДанные(Объект);
	КонецЕсли;
	СообщениеПользователю.Сообщить();
	
КонецПроцедуры // СообщитьОбОшибке()

