
&НаКлиенте
Процедура ОбработкаКоманды(ПараметрКоманды, ПараметрыВыполненияКоманды)
	
	ЭлектронныеДокументыКлиент.venkonСформироватьПодписатьОтправитьЭД(Неопределено, ПараметрКоманды, , ПолучитьСтруктуруДопПараметров());

КонецПроцедуры

&НаСервере
Функция ПолучитьСтруктуруДопПараметров()
	
	Возврат Новый Структура("ВидЭД", Перечисления.ВидыЭД.СчетФактураБезАкта);
	
КонецФункции
