
#Если Клиент Тогда
	
Процедура ПриЗаписи(Отказ)

	Если (НЕ ОбменДанными.Загрузка) И (ЗначениеНеЗаполнено(ВидОбъектаКонтактнойИнформации) ИЛИ ЗначениеНеЗаполнено(Тип)) Тогда

		Предупреждение("Укажите вид и тип объекта контактной информации.");
		Отказ = Истина;

	КонецЕсли;

КонецПроцедуры

#КонецЕсли

Процедура ПриУстановкеНовогоКода(СтандартнаяОбработка, Префикс)
	ДобавитьПрефиксУзла(Префикс);
КонецПроцедуры